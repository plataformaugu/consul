<div>
  <div class="row">
    <div class="small-12 column">
      <%= back_link_to event_path(@event.id) %>

      <h2 class="mt-1">Participar a nombre de otra persona</h2>
      <p>Evento: <strong><%= @event.title %></strong></p>
    </div>
  </div>

  <%=
    render 'shared/manager_form',
    existing_user_path: participate_manager_existing_user_event_path(@event.id),
    new_user_path: participate_manager_new_user_event_path(@event.id)
  %>
</div>
