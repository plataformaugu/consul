<%= render 'header' %>

<div class="row">
  <div class="small-12 column">
    <%= link_to 'Descargar reporte', download_report_admin_stats_path('consultas'), class: 'button' %>
  </div>
</div>

<div class="row stats-numbers">
  <div class="small-12 medium-4 column">
    <p class="featured">
      Procesos totales <br>
      <span class="number"><%= number_with_delimiter(@records.count) %></span>
    </p>
  </div>
  <div class="small-12 medium-4 column">
    <p class="featured">
      Respuestas totales <br>
      <span class="number"><%= number_with_delimiter(@answers) %></span>
    </p>
  </div>
  <div class="small-12 medium-4 column">
  </div>
</div>

<div class="row">
  <div class="small-12 column">
    <% if @records.any? %>
      <table>
        <thead>
          <tr>
            <th scope="col" class="small-1">ID</th>
            <th scope="col">Nombre</th>
            <th scope="col" class="small-2"></th>
          </tr>
        </thead>
        <tbody>
          <% @records.each do |record| %>
            <tr>
              <td><%= record.id %></td>
              <td><%= link_to record.name, poll_path(record.id), target: "_blank" %></td>
              <td><%= link_to 'Ver', polls_admin_stats_path + "/#{record.id}", data: { turbolinks: false } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <%= paginate @records %>
    <% else %>
      <div class="callout primary">
        No hay registros.
      </div>
    <% end %>
  </div>
</div>
