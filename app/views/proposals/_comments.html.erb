<%= render Shared::CommentsComponent.new(@proposal, @comment_tree, !@proposal.proposal_topic.is_active? || !@can_participate) do %>
  <%= render MachineLearning::CommentsSummaryComponent.new(@proposal) %>
<% end %>
