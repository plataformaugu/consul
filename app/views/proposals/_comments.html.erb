<%= render Shared::CommentsComponent.new(@proposal, @comment_tree, @proposal.is_initiative ? !@proposal.is_active? : false) do %>
  <%= render MachineLearning::CommentsSummaryComponent.new(@proposal) %>
<% end %>
